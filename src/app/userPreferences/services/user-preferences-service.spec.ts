import { UserPreferencesService } from './user-preferences-service';

describe('UserPreferencesService', () => {
  it('should create an instance', () => {
    expect(new UserPreferencesService()).toBeTruthy();
  });
});
